version: "2"

run:
  timeout: 5m
  tests: false

formatters:
  enable:
    - gofmt

# Minimal set: only linters that catch real bugs, not style
linters:
  default: none
  enable:
    - govet
    - errcheck
    - ineffassign
  settings:
    errcheck:
      check-blank: false
