run:
  timeout: 5m
  tests: false

linters:
  enable:
    - gofmt
    - govet
    - errcheck
    - staticcheck
    - ineffassign

linters-settings:
  errcheck:
    check-blank: false

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
