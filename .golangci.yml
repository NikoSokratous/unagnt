version: "2"

run:
  timeout: 5m
  tests: false

formatters:
  enable:
    - gofmt

# Minimal set: govet + ineffassign (errcheck disabled: 50+ unchecked errors to fix)
linters:
  default: none
  enable:
    - govet
    - ineffassign
