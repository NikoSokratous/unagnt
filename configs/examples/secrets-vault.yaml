# Example secrets configuration (Vault backend)
# Use with server/unagntd config

secrets:
  backend: vault
  vault:
    address: https://vault.example.com
    token: ""  # Use VAULT_TOKEN env in production
    mount: secret

# Example: reference secrets in auth config
# auth:
#   providers:
#     - oidc:
#         client_secret: secret:ref:Unagnt/okta#client_secret
