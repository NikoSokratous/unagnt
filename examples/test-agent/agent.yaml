name: test-agent
version: "1.0"
description: Simulates test steps for workflow demo

model:
  provider: openai
  name: gpt-4.1-nano
  temperature: 0.2

tools:
  - name: echo
    version: "1"

max_steps: 3
timeout: 30s
